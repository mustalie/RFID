Import Database:
sqlcmd -S localhost -No -U SA -Q "RESTORE DATABASE [SSR] FROM DISK = N'/var/opt/mssql/backup/SSR_NEW.BAK' WITH FILE = 1, MOVE 'SSR' TO '/var/opt/mssql/backup/ssr.mdf', MOVE 'SSR_LOG' TO '/var/opt/mssql/backup/ssr.ldf'"

Masuk ke SQLSRV:
sqlcmd -S localhost -U sa -No -P 'admin123@'

Show Tables:
use SSR;
SELECT * FROM SYSOBJECTS WHERE xtype = 'U';

Create Table:
CREATE TABLE dbo.Inventory (
    id INT,
    name NVARCHAR(50),
    quantity INT,
    PRIMARY KEY (id)
);


Select Data:
SELECT * FROM dbo.Inventory
WHERE quantity > 152;

Show Columns:
SELECT COLUMN_NAME, IS_NULLABLE, DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = N'MPersediaan'

SELECT COLUMN_NAME, IS_NULLABLE, DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = N'dStock'

--------
PHP MsSQL driver:
https://github.com/Microsoft/msphpsql/releases/tag/v5.9.0

SELECT COLUMN_NAME, IS_NULLABLE, DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = N'EmpDosen'
SELECT COLUMN_NAME, IS_NULLABLE, DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = N'EmployeeMaster'
SELECT COLUMN_NAME, IS_NULLABLE, DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = N'MHSMaster'
SELECT COLUMN_NAME, IS_NULLABLE, DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = N'GropPersediaan'
SELECT COLUMN_NAME, IS_NULLABLE, DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = N'MatKul'
SELECT COLUMN_NAME, IS_NULLABLE, DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = N'MKelas'
SELECT COLUMN_NAME, IS_NULLABLE, DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = N'MovePersediaan'
SELECT COLUMN_NAME, IS_NULLABLE, DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = N'TblPresensi'
SELECT COLUMN_NAME, IS_NULLABLE, DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = N'CHECKINOUT'
SELECT COLUMN_NAME, IS_NULLABLE, DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = N'AdjPersediaan'
SELECT COLUMN_NAME, IS_NULLABLE, DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = N'rooms'
SELECT COLUMN_NAME, IS_NULLABLE, DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = N'tags'




sqlcmd -S localhost -U SA -Q "BACKUP DATABASE [demodb] TO DISK = N'/var/opt/mssql/data/demodb.bak' WITH NOFORMAT, NOINIT, NAME = 'demodb-full', SKIP, NOREWIND, NOUNLOAD, STATS = 10"

sqlcmd -S localhost -No -U SA -Q "RESTORE DATABASE [SSR] FROM DISK = N'/var/opt/mssql/backup/SSR_NEW.BAK' WITH FILE = 1, MOVE 'SSR' TO '/var/opt/mssql/backup/ssr.mdf', MOVE 'SSR_LOG' TO '/var/opt/mssql/backup/ssr.ldf'"

SELECT * FROM SYSOBJECTS WHERE xtype = 'U';

SELECT NID, NIDN, AbsenteeismNo, FullName From EmpDosen;